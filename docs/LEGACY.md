# Legacy prototype note

The original GameStick modification prototype contained destructive file-copy restore and pseudo-flashing code. It is intentionally **not distributed** in safety-first release archives.

Historical source should be retained only in version control or a separate archival package, not beside the active application where it could be run accidentally.
